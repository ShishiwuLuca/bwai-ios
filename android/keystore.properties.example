# 复制为 android/keystore.properties 并填写（该文件已加入 .gitignore，勿提交）
# 将 fundex.jks 放在 android/ 目录下（与 storeFile 路径一致）
storeFile=fundex.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=fundex
keyPassword=YOUR_KEY_PASSWORD
